version: '3.9'

services:
  app:
    build:
      context: ./
      dockerfile: ./build/Dockerfile
    ports:
      - "8085:8085"
    restart: unless-stopped
    networks:
      - network
networks:
  network:
    driver: bridge
    name: expression-network
volumes:
  expression-volume: